- args:
    sql: CREATE TABLE "participants"("ptid" serial NOT NULL, "pid" integer NOT NULL,
      "eid" integer NOT NULL, "confirmed" integer NOT NULL, PRIMARY KEY ("ptid") )
  type: run_sql
- args:
    name: participants
  type: add_existing_table_or_view

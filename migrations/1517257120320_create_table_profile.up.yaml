- args:
    sql: CREATE TABLE "profile"("pid" integer NOT NULL, "uid" integer NOT NULL, "name"
      text NOT NULL, "phote" text NOT NULL, "verified" boolean NOT NULL, PRIMARY KEY
      ("pid") )
  type: run_sql
- args:
    name: profile
  type: add_existing_table_or_view
